.venv/
__pycache__/
*.py[cod]
.env
# Qualunque sqlite locale, non solo db.sqlite3: e2e_db.sqlite3 è quasi un mega
# di dati di prova (clienti, telefoni, hash di password) e finirebbe nell'immagine.
*.sqlite3
media/
staticfiles/
.DS_Store
# Script di prova: servono solo in locale (E2E su DB throwaway), mai in produzione.
scripts/
